import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  buttonContainer: {
    flex: 1,
    borderWidth: 1,
    borderColor: 'black',
    borderRadius: 20,
    padding: 10,
    margin: 5,
    justifyContent: 'center',
  },
  buttonText: {
    fontSize: 15,
    textAlign: 'center',
    fontWeight: 'bold',
  },
});
